spring:
  datasource:
    url: jdbc:mysql://blood-donation.c9zurdezvbur.ap-northeast-1.rds.amazonaws.com:3306/blood_donation?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: shyu0220

  mvc:
    hidden-method:
      filter:
        enabled: 'true'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  thymeleaf:
    cache: true
    prefix: classpath:/templates/

server:
  port: 8086

